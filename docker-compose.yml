version: "3"
services:
  redis:
    container_name: redis
    image: redis
    ports:
      - "6379:6379"

  nginx:
    container_name: nginx
    build:
      dockerfile: dockerfile
      context: ./nginx
    ports:
      - "80:80"
    restart: always